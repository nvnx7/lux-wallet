import { Layout } from 'components/layout';
import { GetStarted } from 'components/onboard';

const Onboard = () => {
  return (
    <Layout>
      <GetStarted />
    </Layout>
  );
};

export default Onboard;
